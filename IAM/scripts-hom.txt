DEPLOY:
    cd $env:UpadorArquivoIamIacPath | aws cloudformation deploy --template-file iac.yaml --parameter-overrides file://parameters-hom.json --profile profile-hom --stack-name julucin-role-s3-salvar-arquivos-periodicamente --capabilities CAPABILITY_NAMED_IAM

DELETE:
    aws cloudformation delete-stack --stack-name julucin-role-s3-salvar-arquivos-periodicamente --profile profile-hom